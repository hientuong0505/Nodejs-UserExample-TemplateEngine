<h1>Users</h1>
{{#if hasUsers}}
    <ul>
        {{#each users}}
            <li>
                {{ this.name }}
            </li>
        {{/each}}
    </ul>
{{else}}
     <h1>No users found</h1>
{{/if}}